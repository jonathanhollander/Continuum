{
    "name": "continuum-saas-root",
    "private": true,
    "engines": {
        "node": ">=22.12.0"
    },
    "scripts": {
        "build:frontend": "cd frontend && npm install && npm run build"
    }
}